# Player card concept previews

Concept-review artifacts only. No player-card implementation is included here.

- Direction A: Tournament Cut
- Direction B: Premium Crest
- Direction C: Velocity Frame

Each board shows the Silver, Gold, Shiny Gold, and Prismatic material tiers using the same fictional player and the same information hierarchy.

`exact-proportions-preview.html` is the corrected code-native concept board. It uses the current on-pitch card ratio (96 × 124 px), the existing asymmetric contour, a square portrait slot, and only rating, position, image, and player name inside each card.
